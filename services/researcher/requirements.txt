gpt-researcher==0.16.0
# Keep the OpenAI-compatible client stack aligned with the verified SQLite
# runtime. GPT Researcher leaves these transitive dependencies unpinned,
# which otherwise lets a Docker rebuild silently change request behavior.
openai==2.51.0
langchain-openai==1.4.1
# GPTR 0.16.0 imports this package for its DuckDuckGo retriever but omits it
# from the published dependency metadata.
ddgs==9.14.4
# 1.93.0 is sdist-only on Windows and its Rust metadata build is broken.
# 1.91.4 is the newest compatible release with a Windows-independent wheel.
litellm==1.91.4
fastapi==0.116.1
uvicorn[standard]==0.35.0
pydantic==2.11.7
python-dotenv==1.2.2
aiohttp==3.14.3
# Pinned transitive of openai/langchain; also the client for the OCR sidecar.
httpx==0.28.1
playwright==1.61.0
# Local-document and fetched-PDF parsing: one Rust engine, 14 formats to
# Markdown. Pinned so a Docker rebuild cannot silently change extraction output.
firecrawl-anydoc==0.2.4
# Readability-style boilerplate stripping for fetched HTML sources. Both are
# GPT Researcher transitive deps; pinned here because source_access.py now
# imports them directly.
beautifulsoup4==4.15.0
lxml==6.1.3
# PyMuPDF and python-docx stay as pinned GPT Researcher transitive deps and for
# report export (exporter.py); think-tank no longer parses documents with them.
PyMuPDF==1.28.0
mistune==3.3.4
python-docx==1.2.0
reportlab==5.0.0
